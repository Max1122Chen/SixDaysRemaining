---
description: C# 代码风格规范（Unity 项目）
globs: **/*.cs
alwaysApply: false
---

# Unity C# 代码风格

# 命名
- 标识符、类型名、方法名、属性名、代码里的英文关键字尽量使用英文（例如 `PlayerController` / `Health`）。
- 类型（`class/struct`）以及 `public` 方法/属性使用 `PascalCase`。
- 除属性（property）外的字段（field）以及普通变量/参数统一使用 `camelCase`。
  - 私有字段不强制使用下划线前缀（例如 `private float moveSpeed;`）。

# 结构与 Unity 习惯
- 一个 `MonoBehaviour` 只承担一种职责。
- 倾向用组合（composition）替代继承（inheritance）。
- 当缺失 Inspector 引用会导致运行失败时，在 `Awake()` 或 `OnValidate()` 校验并让问题显性化。
- 让可调参通过 `[SerializeField] private` 暴露；避免用大量 `public` 字段直接暴露。
- 避免 magic number：把可调数值做成可序列化字段或常量。
- `Update()` 保持瘦，把可复用逻辑抽到小的私有方法中。
- 当你觉得划分有价值时，把“输入读取 / 状态变更 / 表现反馈”分开。

# 单例、Manager / SubSystem（由你决定）
- 允许使用单例：全局系统、Manager/ SubSystem 都可以随意用单例。
- 在你还没有明确告诉我要做的“具体系统”偏向管理含义之前，我不会替你决定用 `Manager` 还是 `SubSystem`。
- 我可以在分析时给出选择建议，但需要等你确认后再落地命名/结构。

# 注释语言
- 代码注释默认用中文描述。
- 当注释需要点名“函数/变量/类型”等标识符时，允许直接使用英文标识符（不强制翻译）。

# 原型期目标
- 原型期优先保证可读性与安全迭代，而不是过早追求抽象深度。
